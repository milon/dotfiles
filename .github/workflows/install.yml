name: dotfiles smoke test

on: [push]

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        repository: milon/docfiles
        path: .dotfiles

    - name: Execute full install
      run:  ./install.sh
